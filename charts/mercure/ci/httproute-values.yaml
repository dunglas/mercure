# Test with HTTPRoute enabled
httpRoute:
  enabled: true
  parentRefs:
    - name: gateway
      sectionName: http
  hostnames:
    - mercure-example.local
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /.well-known/mercure
